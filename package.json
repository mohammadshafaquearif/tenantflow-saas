{
  "name": "tenantflow-saas",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise multi-tenant B2B SaaS platform with schema isolation, JWT+RBAC, and CI/CD",
  "scripts": {
    "dev": "npm run dev --workspace=backend & npm run dev --workspace=frontend",
    "dev:api": "npm run dev --workspace=backend",
    "dev:ui": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "start": "npm run start --workspace=backend",
    "test": "npm run test --workspace=backend",
    "test:ci": "npm run test:ci --workspace=backend",
    "lint": "npm run lint --workspace=backend",
    "migrate": "npm run migrate --workspace=backend",
    "vercel-build": "npm run build --workspace=backend && npm run build --workspace=frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.2"
  }
}
